{
  "name": "thecollegecypress",
  "version": "1.0.0",
  "description": "The College Cypresss Imersao",
  "scripts": {
    "cy:open": "cypress open --env env=local ",
    "cy:run": "cypress run --browser chrome --headless --env env=local --config-file cypress.config.ts",
    "cy:run:qa": "cypress run --browser chrome --headless --env env=qa --config-file cypress.config.ts",
    "allure:results": "allure serve allure-results"
  },
  "author": "Nathan Araujo",
  "license": "MIT",
  "dependencies": {
    "cypress": "^13.13.0"
  },
  "devDependencies": {
    "@faker-js/faker": "^8.4.1",
    "allure-commandline": "^2.29.0",
    "allure-cypress": "^3.0.0-beta.7",
    "mochawesome": "^7.1.3",
    "mongoose": "^8.5.3",
    "typescript": "^5.5.3"
  }
}
